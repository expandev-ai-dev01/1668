import { Router } from 'express';

const router = Router();

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// INTEGRATION POINT FOR FEATURES
// All feature-specific routes should be imported and used here.
// Example:
// import featureARoutes from '@/api/v1/internal/feature-a/routes';
// router.use('/feature-a', featureARoutes);
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

export default router;
